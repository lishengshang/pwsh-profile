# ==============================================================
# PowerShell 配置仓库 .gitignore
# ==============================================================

# 第三方模块（pwsh 默认用户模块目录恰好位于仓库内，因为仓库即 $PROFILE 目录；
# 通过 setup.ps1 / Install-Module 安装，不纳入版本管理）
Modules/

# PSReadLine 历史记录
ConsoleHost_history.txt

# setup.ps1 自动备份目录（本机直用仓库时备份会落在仓库内）
backup-*/

# 临时 / 缓存文件
*.log
*.tmp
*starship-init-cache*
*zoxide-init-cache*
*fnm-init-cache*

# AI 工具本地状态（会话/计划等运行时数据，不属于项目约定）
# 注意：AGENTS.md、CLAUDE.md、.trae/rules/ 是要提交的约定文件，不要加进来
.zcode/
.claude/settings.local.json
.codex/

# 编辑器 / 操作系统杂项
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
